import PageContainer from "@/components/ui/PageContainer";
import React from "react";

type Props = {};

const Home = (props: Props) => {
  return (
    <PageContainer>
      <div>HOME</div>
    </PageContainer>
  );
};

export default Home;
